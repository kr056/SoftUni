const homeHandler = require('./home');
const genreHandler = require('./genre');
const memeHandler = require('./meme');

module.exports = {
    home:homeHandler,
    genre:genreHandler,
    meme:memeHandler
};
